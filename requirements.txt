# ==========================================
# Aottar AI Backend - Python dependencies
# Install with: pip install -r requirements.txt
# ==========================================

# --- Web framework / server ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- HTTP / WooCommerce ---
requests==2.34.2

# --- Scheduling (WooCommerce -> FAISS product sync) ---
apscheduler>=3.10,<4.0

# --- LLM / RAG stack ---
langchain==1.3.13
langchain-core==1.4.9
langchain-community==0.4.2
langchain-groq==1.1.3
langchain-huggingface==1.2.2
groq==0.37.1

# --- Search / retrieval ---
rank-bm25==0.2.2
faiss-cpu==1.14.3
sentence-transformers==5.6.0
transformers==5.13.1
torch==2.13.0

# --- Language detection / NLP (used by legacy/optional modules) ---
langdetect==1.0.9
spacy==3.8.14

# NOTE: spaCy's English model is NOT a normal PyPI package and can't be
# listed above - install it separately after the requirements above:
#   python -m spacy download en_core_web_sm
# (this installs en_core_web_sm==3.8.0, matching this project)
